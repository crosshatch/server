import { Selectable } from "kysely"
import { Agents } from "schema"
import { Context } from "../../Context.ts"

export function create(_ctx: Context, _args: "TODO"): Promise<Selectable<Agents>> {
  throw 0
}
